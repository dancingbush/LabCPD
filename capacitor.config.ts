import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mooneycallans.www',
  appName: 'LabCPD',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
